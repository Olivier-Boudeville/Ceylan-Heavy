# erlang not listed here, as now stored in separated repositories, for
# modularity:
#
SUBDIRS = python shell

# These are the subdirectories which will be distributed with 'make dist':
DIST_SUBDIRS = $(SUBDIRS)


all: all-local

